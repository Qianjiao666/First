# 任务管理 Mock 工作台实施计划

## 目标

新增与现有系统隔离的任务管理 mock 页面和交互，复用现有主题、会话、头像和敏感词守卫；不修改 Supabase schema、Edge Functions、论坛或现有任务 API。

## 阶段

- [x] 1. 阅读 HANDOFF、现有任务架构、权限与敏感词实现
- [x] 2. 建立 mock 数据仓库、角色守卫和通用 UI 基础
- [x] 3. 实现管理员工作台与批改详情页
- [x] 4. 实现学生发布、任务广场、我的任务页
- [x] 5. 接入敏感词实时检测、草稿、预览、导出与移动端交互
- [x] 6. 更新测试、变更日志、交付文档并进行视觉与安全审计
- [x] 7. 提交并推送 GitHub（远程认证可用时）

## 不可变边界

- 不修改 `supabase/`、现有任务 API/运行时、论坛、登录、头像上传或主题控制器。
- mock 数据只存于浏览器 `localStorage`，不产生真实任务、声望或数据库写入。
- 管理页面仅对 `ADMIN`/`MODERATOR` 真实会话角色开放；无权限用户重定向至任务大厅。

## 验收

- 五个 mock 页面具备请求中的关键 UI 和交互。
- 全部自主输入使用 `guardFormData`/`inspectXss`，不通过 `innerHTML` 写入用户内容。
- 明暗主题、窄屏布局、错误反馈、过渡动画可用。
- 所有新增测试通过，运行安全审计并记录结果。
