# MKJ v1.1 Delivery Report

Date: 2026-08-13

Branch: `codex/forum-admin-enhancements`

Target: `https://dsxnb.com/MKJ/`

## Delivered scope

- Cross-page Supabase session initialization and global auth-state synchronization.
- Login prompt for protected forum and task actions; expired sessions request re-login.
- Frontend and backend XSS guards for public text inputs.
- Code-only DFA sensitive-word filtering across forum, tasks, categories, announcements, shop and related public text surfaces.
- Three themes with local persistence: tech light, tech dark and fresh blue.
- Shared modal, table, responsive navigation and mobile presentation rules.
- Existing avatar rendering with approved public URLs and text fallback.
- Avatar database, Storage and fail-closed moderation code retained for a future enabled release.

## Deferred scope

Avatar upload is not part of this delivery. The settings dialog does not expose a file input or upload command.

The real Tencent IMS request reached the service but returned:

```text
UnauthorizedOperation.Unauthorized
```

Tencent documents this as missing service permission, no valid package or account arrears. The upload backend remains fail closed and rejected requests did not create a Storage object. Re-enable the browser upload only after the Tencent service is active, the credential owner has `QcloudIMSFullAccess`, and real Pass/Review/Block smoke tests succeed.

## Security status

- Open Critical findings: 0
- Open High findings: 0
- Open Medium findings: 0
- Avatar upload: deferred, unavailable in the browser, backend remains fail closed
- No new database table was created; only the approved nullable `user_public_profiles.avatar text` field exists.
- The avatars bucket remains public read with no browser-role insert, update or delete policy.
- Gift-code reward and authorization logic remains server authoritative.

## Release artifacts

```text
deployment/MKJ-community-forum-tasks-20260812-static-v1.1.zip
SHA-256: 77E7A6D021C62F509FBC6498B3178DEA5BE53DBA2C3D702E06FB851B9E61D2F5

deployment/MKJ-community-forum-tasks-20260812-backend-v1.1.zip
SHA-256: 6C33651B4A750F8FBB651C8A2CAEAE92566542ABB9DED2A029B0A58D4B43F6A4
```

The static archive contains 89 manifest entries. The outer SHA-256 and every internal manifest hash were rechecked before deployment, with zero mismatch. The archive contains no `supabase`, `tests`, `docs` or `output` path.

## Verification evidence

- Full Node regression: 227 passed, 0 failed.
- Release security subset: 21 passed, 0 failed.
- Production JavaScript syntax: 123 checked, 0 failed.
- Static release manifest: 89 checked, 0 mismatch.
- Local browser matrix: 9 representative desktop/mobile screenshots completed with HTTP 200 and no visible overlap or horizontal overflow.
- `git diff --check`: clean for the delivery files.

## Production status

`v1.1` was deployed to `https://dsxnb.com/MKJ/` on 2026-08-13 after Tencent Cloud OrcaTerm MFA verification. The atomic cutover changed only `/var/www/MKJ`.

Deployment evidence:

- Live manifest: `Release: 20260812`, `Revision: v1.1`, `Files: 89`.
- Static archive SHA-256: `77E7A6D021C62F509FBC6498B3178DEA5BE53DBA2C3D702E06FB851B9E61D2F5`.
- Rollback directory: `/var/www/.mkj-community-releases/20260813-094758.previous`.
- Backup archive: `/var/www/.mkj-community-releases/20260813-094758.before-v1.1.tar.gz`.
- HTTP smoke: the main site and all checked MKJ routes returned HTTP 200.
- Production browser matrix: 80 route/viewport combinations, 0 errors, 0 overflows; every checked route reported one learning-demo disclaimer.
- The main site remains the original Next.js application and does not load MKJ release assets. Nginx, Supabase schema/functions, and the backend ZIP were not changed or deployed.

The deployment script used these safeguards:

- change only `/var/www/MKJ`;
- verify the uploaded ZIP checksum and all 89 manifest entries;
- reject forbidden archive paths and any deferred avatar upload entry;
- create a timestamped tar backup and `.previous` rollback directory;
- restore the prior directory automatically if the atomic switch fails.

## Delivery checklist

- [x] Assessment, forum and task source regression suite passes.
- [x] Three-theme selection and local persistence are covered by tests.
- [ ] Avatar upload and real Tencent IMS moderation: deferred by product decision.
- [x] Cross-page session and token-expiry handling are covered by tests.
- [x] Table and modal UI contracts are covered by tests.
- [x] XSS frontend/backend coverage is enforced by tests.
- [x] Global DFA frontend/backend coverage is enforced by tests.
- [x] Security audit has no open High or Medium code finding.
- [x] CHANGELOG versions remain continuous from `v0.1` through `v1.1`.
- [x] Visual transitions stay within the approved 180-250ms range or reduced-motion mode.
- [x] Mobile navigation and task-table reflow are covered by tests.
- [x] Every production page retains the learning-demo disclaimer.
- [x] Tencent Cloud v1.1 static cutover completed with rollback artifacts retained.
- [x] Production HTTP and browser smoke checks completed with 0 errors and 0 overflows.

## Disclaimer

本项目仅为学习演示Demo，请勿直接线上投入正式生产使用。
